# Security Report — 2026-09-07

## Tools Run

| Tool | Status | Finding count |
|---|---|---|
| Gitleaks 8.30.1 (git history + fs) | OK | 0 |
| Bandit | N/A (no `.py` files) | — |
| Semgrep 1.176.1 (OWASP top-ten) | OK | 0 |
| Semgrep 1.176.1 (TypeScript) | OK (0 `.ts`/`.tsx` files) | 0 |
| Semgrep 1.176.1 (secrets) | OK | 0 |
| Trivy 0.74.0 (fs) | OK | 0 |
| TruffleHog 3.97.4 (git) | OK | 22 unverified, 0 verified |
| OSV-Scanner 2.5.1 | OK | 0 |
| mcps-audit | SKIPPED (no MCP config files in repo) | — |
| config-audit.py | OK | 15 MEDIUM/LOW (all false-positive pattern matches on doc/test prose, informational) |
| skill-audit.sh | SKIPPED (90 skill-fork copies; scanner script stalls on batch runs — see Known Remaining Issues) | — |
| mcp-exfil-scan.sh | OK | 0/100 CLEAN |

## Findings

- **TruffleHog — 22 unverified "secrets"**: all are placeholder credential URLs (`http://:secret@host.com`, `https://user:pass@example.com`, `https://user:p%40ss@example.com`) inside `tests/detect-url-launch.test.mjs` test fixtures. 0 verified — not live credentials.
- **config-audit.py — 15 MEDIUM/LOW findings** on `AGENTS.md`, `CLAUDE.md`, `claude.md`, `.claude/settings.json`: keyword pattern matches ("skip verification", ".env file access", "password-related access") on ordinary developer-documentation prose (test-runner instructions, architecture notes) and a broad-matcher `Stop` hook that only runs the bundled `impeccable` CLI. No actual injection or credential-exfiltration pattern confirmed. No repo files require changes.
- **OSV-Scanner / Trivy / Semgrep / Gitleaks**: 0 findings — the `package.json` `overrides` from the 2026-09-06 audit still cover the full transitive-dependency CVE set; `bun.lock` regenerated by upstream's own commits stayed clean.

## Fixes Applied

None required — no exploitable or fixable findings this cycle.

## Known Remaining Issues

- `skill-audit.sh` hangs when driven in a shell loop across many skill files (90 provider-fork copies of the same `impeccable` SKILL.md in this repo's `.claude/`, `.cursor/`, `.gemini/`, etc. directories) — confirmed working standalone on a single file (5/100 LOW RISK, APPROVE) in isolation. Root cause not chased further this cycle; `mcp-exfil-scan.sh`'s skill-level exfiltration-chain check (0/100 CLEAN, 38 skill files scanned) covers the same attack surface for this run.
